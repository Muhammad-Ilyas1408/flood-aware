# Base URL of the Flood-Aware FastAPI backend (backend/app/main.py), with no
# trailing slash. Include any deployment-specific API prefix (see the
# backend's `api_prefix` setting) -- it defaults to empty, so local dev
# usually points straight at the unprefixed routes.
NEXT_PUBLIC_API_BASE_URL=http://localhost:8000
